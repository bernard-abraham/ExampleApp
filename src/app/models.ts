export interface Joke {
    type: string;
    setup: string;
    punchline: string;
    id: number;
  }

  export interface APIResponse<T> {
    type: string;
    setup: string;
    punchline: string;
    id: number;
}